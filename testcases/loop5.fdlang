x = 100;

while (i <= 10) {
    y = input();
    if (y < 128) {
        x = x - 1;
    } else {
        x = x + 1;
    }
    i = i + 1;
}

check_interval(x, 89, 111);
check_interval(x, 90, 110);
check_interval(x, 91, 109);