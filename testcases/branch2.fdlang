function main(){
x = input();

if (x < 128) {
    y = 1;
} else {
    y = 2;
}

if (y == 1) {
    check_interval(x, 0, 127);
    x = 1;
} else {
    check_interval(x, 128, 255);
    x = 2;
}

check_interval(y, 1, 2);
check_interval(x, 1, 2);
}